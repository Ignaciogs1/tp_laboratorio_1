#ifndef CALCULADORAFUNCIONES_H_INCLUDED
#define CALCULADORAFUNCIONES_H_INCLUDED

int suma (float x,float y);
/** \brief Recibe los dos operandos que se ingresaron y se calcula la suma de los mismos.
 * \param Primer operando de tipo float que fue ingresado por el usuario.
 * \param Segundo operando de tipo float que fue ingresado por el usuario.
 * \return Retorna el resultado de la suma de los operandos ingresados.
 */

int resta (float x,float y);
/** \brief Recibe los dos operandos que se ingresaron y se calcula la resta de los mismos.
 * \param Primer operando de tipo float que fue ingresado por el usuario.
 * \param Segundo operando de tipo float que fue ingresado por el usuario.
 * \return Retorna el resultado de la resta de los operandos ingresados.
 */

float division (float x,float y);
/** \brief Recibe los dos operandos que se ingresaron y se calcula la división de los mismos.
 * \param Primer operando de tipo float que fue ingresado por el usuario.
 * \param Segundo operando de tipo float que fue ingresado por el usuario.
 * \return Retorna el resultado de la división de los operandos ingresados.
 */

int multiplicacion (float x,float y);
/** \brief Recibe los dos operandos que se ingresaron y se calcula la multiplicación de los mismos.
 * \param Primer operando de tipo float que fue ingresado por el usuario.
 * \param Segundo operando de tipo float que fue ingresado por el usuario.
 * \return Retorna el resultado de la multiplicación de los operandos ingresados.
 */

long int factorial (float x);
/** \brief Recibe los dos operandos que se ingresaron y se calcula el factorial de los mismos.
 * \param Primer operando de tipo float que fue ingresado por el usuario.
 * \return Retorna el factorial de los dos operandos ingresados.
 */

#endif // CALCULADORAFUNCIONES_H_INCLUDED
